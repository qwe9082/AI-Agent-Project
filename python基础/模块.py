# 模块modole
# 内置模块
# match(数学计算) random(随机数) os(操作系统) sys(系统参数)
# datetime(日期时间) time(时间访问) re(正则) csv(csv文件操作)
import math
import random as rd
# from os import chown,chdir
from sys import argv as argv
from datetime import *
import time
import re
import csv

# 模块中测试代码
# __name__ Python中的内置变量，表示当前模块的名字
if __name__ == '__main__':
    print('-' * 30)
print(__file__)

print(math.fabs(-5))