# 集合set
# 特点 1.无序、不可重复、可修改
s1 = {1,5,'python', 2.5, True, None}

# 常用方法
# add 添加元素 s1.add(2)
# remove 移除元素 s2.remove(True)
# pop 随机产出集合中的元素 s2.pop()
# clear 清空集合 s2.clear()
# difference 求两个集合的差集 s1.difference(s2)
# union 求两个集合的并集 s1.union(s2)
# intersection 求两个集合的交集 s1.intersection(s2)